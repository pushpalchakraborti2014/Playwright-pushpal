# Environment Variables Template
# Copy this file to .env and update with your values

# Base URL for the application
BASE_URL=https://example.com

# API Base URL
API_URL=https://api.example.com

# Test Environment
TEST_ENV=dev

# Database Configuration (if needed)
DB_HOST=localhost
DB_PORT=3306
DB_USER=testuser
DB_PASS=password
DB_NAME=testdb

# Authentication Tokens (if needed)
API_TOKEN=your-api-token-here
AUTH_TOKEN=your-auth-token-here

# Test User Credentials
TEST_USERNAME=test@example.com
TEST_PASSWORD=Test@123

# Admin User Credentials
ADMIN_USERNAME=admin@example.com
ADMIN_PASSWORD=Admin@123

# Browser Configuration
HEADLESS=true
SLOW_MO=0

# Timeout Configuration
TEST_TIMEOUT=60000
ACTION_TIMEOUT=30000

# Reporting Configuration
ALLURE_RESULTS_DIR=allure-results
SCREENSHOT_MODE=only-on-failure
VIDEO_MODE=retain-on-failure

# CI/CD Configuration
CI=false
WORKERS=2
RETRIES=1

# External Service URLs
EXTERNAL_API_URL=https://external-api.example.com
WEBHOOK_URL=https://webhook.example.com

# AWS Configuration (if using AWS services)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET_NAME=your-s3-bucket

# Email Configuration (for test reports)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Slack Configuration (for notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your-webhook-url
SLACK_CHANNEL=#testing

# Performance Testing
PERFORMANCE_BUDGET_LOAD_TIME=5000
PERFORMANCE_BUDGET_FCP=3000

# LambdaTest Configuration
LT_USERNAME=pushpalchakraborti2014
LT_ACCESS_KEY=LT_22KXi0gCk9yvDkZTFhf60N7mxt3jsXVOo7dRZhOqU2SylOt
LT_BUILD_NAME=Playwright-Pushpal-Build
LT_PROJECT_NAME=Playwright-Automation
LT_TUNNEL=false
LT_TUNNEL_NAME=
LT_CONSOLE_LOGS=true
LT_NETWORK_LOGS=true
LT_VIDEO=true
LT_SCREENSHOT=true